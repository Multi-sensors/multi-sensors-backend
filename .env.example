SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=
ALLOWED_SUPABASE_USER_ID=
# Optional: falls back to the Supabase Reset Password email template's own redirect
# until the frontend's reset-password page exists.
FRONTEND_RESET_PASSWORD_URL=